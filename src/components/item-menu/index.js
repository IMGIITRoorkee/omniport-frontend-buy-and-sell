import { connect } from 'react-redux'
import ItemMenu from './renderer'

function mapStateToProps(state) {
    return {
    }
}
const mapDispatchToProps = dispatch => {
    return {
        }
};
export default connect(mapStateToProps, mapDispatchToProps)(ItemMenu);