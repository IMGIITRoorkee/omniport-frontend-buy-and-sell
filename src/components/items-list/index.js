import { connect } from 'react-redux'
import ItemsList from './renderer'

function mapStateToProps(state) {
    return {
    }
}
const mapDispatchToProps = dispatch => {
    return {
        }
};
export default connect(mapStateToProps, mapDispatchToProps)(ItemsList);